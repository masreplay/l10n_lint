name: example
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  intl: any
  flutter_localizations:
    sdk: flutter

  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^4.0.0
  custom_lint: ^0.6.7

  l10n_lint:
    path: ../

flutter:
  generate: true
  uses-material-design: true
